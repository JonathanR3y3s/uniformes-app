import{REGLAS,VERSION}from'./config.js';import{getStore}from'./storage.js';import{calcStats,calcStatsArea}from'./rules.js';import{buildAreaBadge,createChart}from'./ui.js';
export function render(){const s=calcStats();const areas=Object.keys(REGLAS);let h='';h+='<div class="page-head"><div class="page-title"><h1>Dashboard</h1><p>Sistema de Uniformes ASSA ABLOY 2026 — v'+VERSION+'</p></div></div>';h+='<div class="kpi-grid"><div class="kpi"><div class="kpi-label">Total Empleados</div><div class="kpi-value">'+s.total+'</div><div class="kpi-sub">'+s.activos+' activos</div></div><div class="kpi success"><div class="kpi-label">Capturados</div><div class="kpi-value">'+s.capturados+'</div><div class="kpi-sub">'+s.pct+'%</div></div><div class="kpi warning"><div class="kpi-label">Pendientes</div><div class="kpi-value">'+s.pendientes+'</div><div class="kpi-sub">'+(100-s.pct).toFixed(1)+'%</div></div><div class="kpi info"><div class="kpi-label">Bajas / Mov / Incap</div><div class="kpi-value">'+s.bajas+'</div><div class="kpi-sub">No requieren captura</div></div></div>';h+='<div class="chart-grid"><div class="chart-box"><h3>Empleados por Área</h3><canvas id="chAreas" height="220"></canvas></div><div class="chart-box"><h3>Estado de Captura</h3><canvas id="chEstado" height="220"></canvas></div></div>';h+='<div class="card"><div class="card-head"><h3>Resumen por Área</h3></div><div class="table-wrap"><table class="dt"><thead><tr><th>Área</th><th>Total</th><th>Capturados</th><th>Pendientes</th><th>Bajas</th><th>Progreso</th></tr></thead><tbody>';areas.forEach(a=>{const st=calcStatsArea(a);const bc=parseInt(st.pct,10)>=80?'var(--success)':parseInt(st.pct,10)>=50?'var(--warning)':'var(--danger)';h+='<tr><td>'+buildAreaBadge(a)+'</td><td>'+st.total+'</td><td class="font-bold" style="color:var(--success)">'+st.capturados+'</td><td style="color:var(--warning)">'+st.pendientes+'</td><td style="color:var(--info)">'+st.bajas+'</td><td><div class="flex items-center gap-2"><div class="progress" style="flex:1"><div class="progress-bar" style="width:'+st.pct+'%;background:'+bc+'"></div></div><span class="text-xs font-bold">'+st.pct+'%</span></div></td></tr>';});h+='</tbody></table></div></div>';return h;}
export function init(){const areas=Object.keys(REGLAS);createChart('chAreas',{type:'bar',data:{labels:areas,datasets:[{data:areas.map(a=>getStore().employees.filter(e=>e.area===a).length),backgroundColor:['#fef3c7','#dbeafe','#d1fae5','#fce7f3','#e0e7ff','#fee2e2','#d1fae5','#fef08a'],borderRadius:6,borderSkipped:false}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{beginAtZero:true,ticks:{precision:0},grid:{color:'#f1f5f9'}},x:{grid:{display:false}}}}});const s=calcStats();createChart('chEstado',{type:'doughnut',data:{labels:['Capturados','Pendientes','Bajas / Mov'],datasets:[{data:[s.capturados,s.pendientes,s.bajas],backgroundColor:['#059669','#d97706','#0284c7'],borderWidth:0,hoverOffset:8}]},options:{responsive:true,maintainAspectRatio:false,cutout:'65%',plugins:{legend:{position:'bottom',labels:{padding:16,usePointStyle:true,font:{size:12,weight:'600'}}}}}});}
